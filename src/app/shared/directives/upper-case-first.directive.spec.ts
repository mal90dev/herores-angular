import { UpperCaseFirstDirective } from './upper-case-first.directive';

describe('UpperCaseFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new UpperCaseFirstDirective();
    expect(directive).toBeTruthy();
  });
});
